# API_BASE_URL is hardcoded to /api/v1 in lib/constants.ts
# Do NOT set NEXT_PUBLIC_API_BASE_URL — it will get baked into the build

# Activity Feed Settings (optional)
NEXT_PUBLIC_ACTIVITY_POLL_INTERVAL=10000
NEXT_PUBLIC_LISTINGS_POLL_INTERVAL=30000
